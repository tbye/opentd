[project]
name = "opentd"
version = "0.1.0"
description = "opentd.org — create browser tower defense games"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0",
    "django-allauth>=65.0",
    "django-anymail[resend]>=15.0",
    "django-htmx>=1.28.0",
    "gunicorn>=26.0.0",
]

[dependency-groups]
dev = []
