# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/

# UV / packaging
*.egg-info/
dist/
build/

# Django
db.sqlite3
*.sqlite3
staticfiles/
mediafiles/
*.log

# Environment / secrets (never commit real keys)
.env
.env.*
!.env.example
.env.local
*.local

# IDE / OS
.idea/
.vscode/
*.swp
*~
.DS_Store

# Tailwind (keep input + plugins; rebuild output)
# static/css/output.css is committed so the app works without a watch process

# Cutover artifacts (SQLite copies / dumpdata fixtures)
var/cutover-*/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.grok/
graphify-out/
